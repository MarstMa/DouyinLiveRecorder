# 开发日志 2026-09-16

## 已完成
- [x] 与用户沟通需求，确认全部关键决策
- [x] 完成开发方案并获用户批准
- [x] 搭建工程管理骨架（docs/、开发日志/、CLAUDE.md）
- [x] 安装开发环境：Python 3.12.10、ffmpeg 9.0.1、PySide6、streamget、send2trash
- [x] 编写核心代码：config / extractor / recorder / scheduler / autostart
- [x] 抓流检测实测通过（返回真实主播名 + 开播状态）
- [x] 修复：添加主播后列表不出现（add_streamer 参数不匹配）
- [x] 修复：抖音号无法识别主播名（webRid 解析）
- [x] 分享链接自动解析（normalize_input：整段分享文字 → 提取链接/抖音号）
- [x] 深色主题（现代深色配色）
- [x] 卡片式主播列表（StreamerCard + 侧边导航）
- [x] 视频库：按主播分组展示已录视频，支持播放/定位/删除到回收站
- [x] 去掉「启用/停用」状态（已停用不再出现，主播一律自动启用）
- [x] 主播卡片改为头像 + 名字 + 缩略信息，点击卡片进详情
- [x] 「启用/停用」重新加入并清晰化：详情开关 + 卡片置灰角标
- [x] 主播卡片方形网格排列，点击进详情子界面（可编辑）
- [x] 抖音头像：抓取头像 URL 并下载到本地，圆形裁剪显示
- [x] 修复：分享链接（v.douyin.com）也能抓到抖音头像
- [x] 分离「停用」与「自动检测与录制」两个独立开关（停用后不能手动录制；关自动后仍可手动）
- [x] 默认改为手动模式（不自动录制）；检测照常进行，仅自动录制受开关控制
- [x] 手动录制改为复用后台已检测到的直播信息，避免重复请求触发风控
- [x] 手动录制报错区分「检测失败」与「未开播」
- [x] 卡片状态优先级：录制中 > 直播中（不再被顶掉）
- [x] 添加/编辑主播后立即刷新检测状态（不用等检测间隔）
- [x] 错误链接识别收紧：非抖音链接/截断链接不再误认主播
- [x] 抖音号识别重做：通过重定向判断开播，不再误抓推荐房间；未开播时从页面提取真实昵称/头像
- [x] 抖音号开播状态检测改用页面房间状态解析（fetch_web_stream_data_v1），开播/未开播都能正确判断
- [x] 界面改为三级导航：卡片网格 → 主播页（概览 + 该主播视频）→ 编辑页（表单 + 开关）
- [x] 主播页内查看该主播已录视频（播放/定位/删除到回收站）
- [x] 去掉侧边栏与全局「视频库」页；运行日志折叠成小按钮
- [x] 添加/编辑主播后立即检测单个主播状态（check_one_now）
- [x] 「启用该主播」开关移到主播页（二级）
- [x] 主播列表排序：录制中 > 直播中 > 未开播 > 已停用
- [x] 调度器改为多主播同时录制（去掉「同一时间只录一个」与弹选）
- [x] 重新启用主播时立即单独检测一次（修复「重新启用后不检测」）
- [x] 去掉标题栏「停止检测」按钮，检测始终运行
- [x] 停用主播时立即停止其录制（手动停止改为后台线程，不卡界面）
- [x] 一级卡片新增「自动录制/手动录制」标识 + 状态扫描时间
- [x] 编译、导入、启动测试均通过

## 待办
- [ ] 用真实开播的直播间端到端测试：开播自动录制 → 下播自动保存 mp4
- [ ] 阶段 5：打包成 .exe（PyInstaller）
- [ ] 阶段 6：录屏备用模式（次要）
- [ ] 补充 git 版本管理（本机未装 git，需先安装）
